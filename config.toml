# The URL the site will be built for
base_url = "https://zhenkyle.github.io/emacs_journal"
title = "Emacs Journal"
description = "My Emacs journal"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true
theme = "hyde"

taxonomies = [
    {name = "tags", rss = true}
]

[extra]
# Put all your custom variables here
# Put all your custom variables here
# BEGIN for hyde
#hyde_sticky = true
#hyd_reverse = false
hyde_theme = "theme-base-09"
hyde_links = [
    {url = "https://github.com/zhenkyle/emacs_journal", name = "Edit"},
    {url = "https://travis-ci.org/zhenkyle/emacs_journal", name = "CI status"},
]
# END for hyde

